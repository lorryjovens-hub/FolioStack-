[railway]
  version = 2

[build]
  command = "cd server && npm install"
  rootDirectory = "server"

[deploy]
  startCommand = "npm start"
  region = "singapore"
  runtime = "node"

[environment]
  NODE_ENV = "production"
