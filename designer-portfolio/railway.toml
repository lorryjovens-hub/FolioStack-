[railway]
  version = 2

[build]
  command = "cd server && npm install"
  outputDirectory = "server"

[deploy]
  startCommand = "npm start"
  region = "singapore"
  runtime = "node"

[environment]
  NODE_ENV = "production"
